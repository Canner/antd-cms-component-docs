## Array Gallery

```jsx
<array
  keyName="gallery"
  imageServiceConfig={new ImgurService({
    mashapeKey: '<mashapeKey>',
    clientId: '<clientId>'
  })}
  value={immutable.fromJS([{
    image: "<your image>"
  }, ...])}
  ui="gallery"/>
```
